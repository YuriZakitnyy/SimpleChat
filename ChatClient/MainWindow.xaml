<Window x:Class="ChatClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chat Client" Height="450" Width="700">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Expander Header="Connection Settings" IsExpanded="True" Margin="0,0,0,8">
            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" Margin="5">
                <Button DataContext="{Binding ConnectCommand}" Command="{Binding}" Content="{Binding Header}" IsEnabled="{Binding Enabled}" Width="80" Margin="0,0,8,0" />
                <Button DataContext="{Binding DisconnectCommand}" Command="{Binding}" Content="{Binding Header}" IsEnabled="{Binding Enabled}" Width="80"/>
                <Label Content="Hub URL:" VerticalAlignment="Center" Margin="16,0,4,0" />
                <ComboBox ItemsSource="{Binding Addresses}" Text="{Binding BackendUrl, Mode=TwoWay}"
                      Width="220" Margin="12,0,8,0" VerticalAlignment="Center" IsEditable="True" IsTextSearchEnabled="True"/>
                <Label Content="User:" VerticalAlignment="Center" />
                <TextBox Text="{Binding UserName,Mode=TwoWay}" Width="100" Margin="6,0,12,0" VerticalContentAlignment="Center" />
            </StackPanel>
        </Expander>
        
        <ListBox x:Name="MessagesListBox" Grid.Row="1" ItemsSource="{Binding Path=Messages}" Margin="0,0,0,8"
                 ItemTemplate="{StaticResource ChatMessageDataTemplate}"
                ItemContainerStyle="{StaticResource ChatItemContainerStyle}"
                ScrollViewer.CanContentScroll="True"
                VirtualizingStackPanel.IsVirtualizing="True"
                VirtualizingStackPanel.VirtualizationMode="Recycling"
                VirtualizingStackPanel.ScrollUnit="Pixel"/>

        <Grid Grid.Row="2" Margin="0,0,0,0" Height="60">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Text="{Binding MessageText,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,0"
                 VerticalContentAlignment="Top"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Disabled"
                 AllowDrop="True" 
                 PreviewDragOver="FilePathTextBox_PreviewDragOver" 
                 Drop="FilePathTextBox_Drop" >
                <TextBox.CommandBindings>
                    <CommandBinding Command="ApplicationCommands.Paste"
                                    Executed="MessageTextBox_PasteExecuted"
                                    CanExecute="MessageTextBox_PasteCanExecute"/>
                </TextBox.CommandBindings>
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter" Modifiers="Control" Command="{Binding SendCommand}"/>
                </TextBox.InputBindings>
            </TextBox>
            
            <!-- Emoji Picker ComboBox -->
            <StackPanel Grid.Column="2" Orientation="Vertical" Margin="5,0,0,0">
                <ComboBox
                          ItemsSource="{Binding Emojis}"
                          SelectedItem="{Binding SelectedEmoji, Mode=TwoWay}"
                          Width="50"
                          Height="30"
                          Margin="0,0,0,0"
                          ToolTip="Select emoji to send">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Image Source="{Binding Image}" Width="32" Height="32" Margin="2"/>
                                <TextBlock Text="{Binding Name}" VerticalAlignment="Center" Margin="5,0,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                    <ComboBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel />
                        </ItemsPanelTemplate>
                    </ComboBox.ItemsPanel>
                    <!-- Button template to show emoji icon -->
                    <ComboBox.Template>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid>
                                <ToggleButton x:Name="ToggleButton"
                                            Grid.Column="2"
                                            Focusable="false"
                                            IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                            ClickMode="Press">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border Background="{TemplateBinding Background}"
                                                  BorderBrush="{TemplateBinding BorderBrush}"
                                                  BorderThickness="1"
                                                  CornerRadius="2">
                                                <TextBlock Text="{Binding Path=SelectedEmoji.Emoji}" 
                                                         FontSize="20" 
                                                         HorizontalAlignment="Center" 
                                                         VerticalAlignment="Center"
                                                         Margin="5,2"/>
                                            </Border>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <Popup x:Name="Popup"
                                     Placement="Top"
                                     IsOpen="{TemplateBinding IsDropDownOpen}"
                                     AllowsTransparency="True"
                                     Focusable="False"
                                     PopupAnimation="Slide">
                                    <Grid x:Name="DropDown"
                                        SnapsToDevicePixels="True"
                                        MinWidth="{TemplateBinding ActualWidth}"
                                        MaxHeight="400"
                                        Width="350">
                                        <Border x:Name="DropDownBorder"
                                              Background="White"
                                              BorderThickness="1"
                                              BorderBrush="Gray">
                                            <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                                <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                            </ScrollViewer>
                                        </Border>
                                    </Grid>
                                </Popup>
                            </Grid>
                        </ControlTemplate>
                    </ComboBox.Template>
                </ComboBox>
                <Button DataContext="{Binding SendEmojiCommand}" Margin="0,5,0,0"  Command="{Binding}" Content="{Binding Header}" IsEnabled="{Binding Enabled}" Height="25"/>
            </StackPanel>
            <Button Grid.Column="1" DataContext="{Binding SendCommand}" Command="{Binding}" Content="{Binding Header}" IsEnabled="{Binding Enabled}" Width="50" />
        </Grid>
    </Grid>
</Window>
